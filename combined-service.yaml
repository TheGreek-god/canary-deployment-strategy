apiVersion: v1
kind: Service
metadata:
  name: online-shop-service
  namespace: canary-deployment
spec:
  selector:
    app: online-shop
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30001
  type: NodePort
